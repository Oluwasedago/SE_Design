// File: src/library/cables/index.ts
// Temporary stub to resolve protocol compatibility errors

export interface BaseCableDefinition {
    cableId: string;
    name: string;
    // Add other basic properties as needed later
}